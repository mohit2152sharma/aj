// Global test setup
import { afterAll, beforeAll } from 'vitest';

// Clean up any lingering timeouts or processes
beforeAll(() => {
    // Any global setup can go here
});

afterAll(() => {
    // Any global cleanup can go here
});
